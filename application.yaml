server:
  port: 8080

spring:
  datasource:
    username: root
    password: polar12Bear
    url: jdbc:mysql://localhost:3306/todopring?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true